# Claude Code Rules

_Extends: ~/AGENTS.md, ~/CLAUDE.md_

hAFS-specific rules for Claude Code.

## Git Commits

- Do NOT add "Generated with Claude Code" or "Co-Authored-By: Claude" signatures unless asked
- Keep commit messages concise and descriptive

## hAFS Development

- Run `hafs` CLI to test changes interactively
- Check `~/.context/` for live AFS state
- Use `hafs.toml` for configuration changes
