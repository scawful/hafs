import warnings
from agents.utility.cartographer import *

warnings.warn(
    'hafs.agents.cartographer is deprecated, use agents.utility.cartographer instead',
    DeprecationWarning,
    stacklevel=2
)
