import warnings
from agents.analysis.embedding_analyzer import *

warnings.warn(
    "hafs.agents.embedding_analysis is deprecated, use agents.analysis.embedding_analyzer instead",
    DeprecationWarning,
    stacklevel=2
)
