import warnings
from agents.analysis.embedding_analyzer import *

warnings.warn(
    "agents.analysis.embedding_analyzer is deprecated, use agents.analysis.embedding_analyzer instead",
    DeprecationWarning,
    stacklevel=2
)
